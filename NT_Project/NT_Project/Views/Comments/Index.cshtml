@model IEnumerable<NT_Project.Models.Comment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Comments</h2>


<table class="table">
    <tr>
        <th>
            Comment
        </th>
        <th>
            Date
        </th>
        <th>
            Commented by
        </th>
       
        <th></th>
    </tr>
   
@foreach (var item in Model) {
    <tr>
        <td>
            @*@if (item.comment_message.Length > 30)
            {
                int i = 29;
                while (i < item.comment_message.Length)
                {
                    @item.comment_message.Insert(i, Environment.NewLine);
                    i = i + 30;
                }


                @Html.DisplayFor(modelItem => item.comment_message)

            }
            else*@
            
           @Html.DisplayFor(modelItem => item.comment_message)
            
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comment_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user_name)
        </td>
        
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>*@
    </tr>
}

</table>
